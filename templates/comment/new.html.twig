{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form, { 'action' : path('comment-add', { post: post.id }) }) }}
    {{ form_widget(form.content) }}
    {{ form_widget(form.send) }}
{{ form_end(form) }}